import 'package:flutter/material.dart';

class BurgerPage extends StatelessWidget {
  static const tag = 'BurgerPage';
  const BurgerPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
